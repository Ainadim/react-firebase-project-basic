import React from 'react';
import PropTypes from 'prop-types';

const Footer = props => {
    return (
        <div>
            <h2>Footer</h2>
        </div>
    );
};

Footer.propTypes = {
    
};

export default Footer;